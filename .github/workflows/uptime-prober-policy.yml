name: uptime-prober manifest import

# Manifest changes on main import a draft through Liskov's GitHub-OIDC route.
# The artifact workflow remains responsible for building, pinning, and attesting
# the runtime bundle; this workflow advances the authored manifest.

on:
  push:
    branches: [main]
    paths:
      - "uptime-prober/.liskov/**"
      - ".github/workflows/uptime-prober-policy.yml"
  workflow_dispatch:

permissions:
  id-token: write
  contents: read

jobs:
  manifest:
    uses: proof-computer/liskov-github-actions/.github/workflows/policy-sync.yml@main
    with:
      application-id: uptime-prober
      manifest-path: uptime-prober/.liskov/uptime-prober.policy.json
